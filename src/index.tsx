import * as React from 'react';
import * as ReactDOM from 'react-dom';
import LikeAppContainer from './Container/LikeAppContainer'

ReactDOM.render(<LikeAppContainer />, document.getElementById('app'));
